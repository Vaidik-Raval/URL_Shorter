<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>URL Shortener</title>

    <style>

        body {
            font-family: 'Poppins', sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background: linear-gradient(135deg, #9bc7bb, #f7c1a0);
            margin: 0;
        }

        .container {
            background: rgba(255, 255, 255, 0.95);
            padding: 30px;
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
            border-radius: 12px;
            width: 450px;
            text-align: center;
            animation: fadeIn 1s ease-in-out;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(-20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        h2 {
            margin-bottom: 15px;
        }

        input[type="url"] {
            width: calc(100% - 20px);
            padding: 12px;
            margin: 12px 0;
            border: 2px solid #ddd;
            border-radius: 6px;
            font-size: 16px;
            transition: 0.3s;
        }

        input[type="url"]:focus {
            border-color: #ff6b81;
            outline: none;
            box-shadow: 0 0 8px rgba(255, 107, 129, 0.5);
        }

        button {
            background: #ffa200;
            color: white;
            padding: 12px 20px;
            border: none;
            border-radius: 6px;
            cursor: pointer;
            font-size: 16px;
            transition: 0.3s;
        }

        button:hover {
            background: #e25565;
            transform: scale(1.05);
        }

        .result {
            margin-top: 20px;
            word-wrap: break-word;
            padding: 10px;
            background: #fff0f3;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        .result a {
            color: #ff6b81;
            font-weight: bold;
            text-decoration: none;
            transition: 0.3s;
        }

        .result a:hover {
            text-decoration: underline;
        }

        .icon {
            font-size: 24px;
            color: #ff6b81;
        }
    </style>
</head>

<body>
    <div class="container">
        <h2><i class="fas fa-cut icon"></i> URL Shortener</h2>

        <form action="/url" method="POST">
            <input type="url" name="url" id="urlInput" placeholder="Enter a long URL" required>
            <button type="submit"><i class="fas fa-compress"></i> Shorten URL</button>
        </form>

        <% if(locals.id) { %>
            <div class="result">
                <p><i class="fas fa-link"></i> Shortened URL:</p>
                <p><a href="http://localhost:3000/<%= id %>" target="_blank">http://localhost:3000/<%= id %></a></p>
            </div>
        <% } %>
    </div>
</body>

</html>
